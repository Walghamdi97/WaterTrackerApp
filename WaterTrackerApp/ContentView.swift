//
//  ContentView.swift
//  WaterTrackerApp
//
//  Created by Wejdan Alghamdi on 23/03/1446 AH.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Hello, world!")
            
            Text("I used app to try the command")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
